# 📋 Инструкция по тестированию VideoMeet

## ✅ Что исправлено:

### 1. Чат
- ✅ Добавлен `roomSlug` в запрос отправки сообщения
- ✅ Исправлена структура типа `ChatMessage` (теперь соответствует серверу)
- ✅ Обновлен компонент Chat для работы с новой структурой

### 2. Участники
- ✅ Исправлена подписка на события - теперь независима от загрузки room
- ✅ Добавлена обработка события `room:joined` с полным списком участников
- ✅ Добавлено детальное логирование для отладки
- ✅ Преобразование данных участников из серверного формата в клиентский

### 3. Видео
- ✅ Исправлена логика переиспользования stream между страницами
- ✅ Добавлены задержки для инициализации socket
- ✅ Retry логика для получения socketId

---

## 🚀 Запуск проекта

### Шаг 1: Запуск баз данных

```powershell
cd server
docker-compose up -d
```

Проверка:
```powershell
docker-compose ps
```

Должны быть запущены:
- `postgres` (порт 5433)
- `redis` (порт 6378)

### Шаг 2: Запуск бэкенда

```powershell
cd server
bun run dev
```

Должно вывести:
```
Server running on port 5000
Database connected
Redis connected
```

### Шаг 3: Запуск фронтенда

Откройте НОВЫЙ терминал:

```powershell
cd client
bun run dev
```

Должно вывести:
```
VITE v7.3.1  ready in X ms
➜  Local:   http://localhost:5173/
```

---

## 🧪 Сценарий тестирования

### Подготовка двух пользователей

**Вариант 1: Два разных браузера** (⭐ РЕКОМЕНДУЕТСЯ)
- Chrome: `http://localhost:5173`
- Firefox/Edge: `http://localhost:5173`

**Вариант 2: Один браузер + инкогнито**
- Обычное окно: `http://localhost:5173`
- Инкогнито (Ctrl+Shift+N): `http://localhost:5173`

---

### 👤 Пользователь 1 (Хост)

#### 1. Регистрация
- Email: `user1@test.com`
- Пароль: `password123`
- Имя: `User One`
- Нажать "Зарегистрироваться"

#### 2. Создание комнаты
- Ввести название: "Test Room"
- Нажать "Создать встречу"

#### 3. Лобби
- Разрешить доступ к камере и микрофону
- Убедиться что видео отображается
- Нажать "Присоединиться к встрече"

#### 4. В комнате
- Открыть консоль (F12) - **ВАЖНО!**
- Проверить логи:
  ```
  [RoomPage] Loading room data for slug: ...
  [RoomPage] Room data loaded: ...
  [RoomPage] Joining room via socket...
  [useSocket] Room joined event received: ...
  [VideoGrid] Rendering: ...
  ```

#### 5. Скопировать slug комнаты
- Из адресной строки: `http://localhost:5173/room/XXXX-XXXX-XXXX`
- Скопировать: `XXXX-XXXX-XXXX`

---

### 👤 Пользователь 2 (Гость)

#### 1. Регистрация
- Email: `user2@test.com`
- Пароль: `password123`
- Имя: `User Two`

#### 2. Присоединение к комнате
- На главной странице: раздел "Присоединиться к встрече"
- Ввести код комнаты: `XXXX-XXXX-XXXX`
- Нажать "Присоединиться"

#### 3. Лобби
- Разрешить доступ к камере и микрофону
- Проверить что видео работает
- Нажать "Присоединиться к встрече"

#### 4. В комнате
- Открыть консоль (F12)
- Проверить логи участников

---

## ✅ Что должно работать

### У обоих пользователей:

#### Участники
- ✅ Счетчик участников: `Участники (2)`
- ✅ При клике на "Участники" видно оба пользователя
- ✅ У хоста должна быть корона 👑

#### Видео
- ✅ Видно 2 видео в сетке (свое + другого пользователя)
- ✅ Имя пользователя под видео
- ✅ "Вы" отмечено у своего видео

#### Аудио/Видео контроли
- ✅ При выключении микрофона:
  - Иконка микрофона красная
  - Другой пользователь видит перечеркнутый микрофон
- ✅ При выключении камеры:
  - Показывается аватар с буквой
  - Другой пользователь не видит видео

#### Чат
- ✅ Открывается без белого экрана
- ✅ Можно написать сообщение
- ✅ Сообщение приходит другому пользователю
- ✅ Свои сообщения фиолетовые справа
- ✅ Чужие сообщения серые слева

#### Расшаривание экрана
- ✅ Кнопка "Поделиться экраном" работает
- ✅ Появляется выбор окна/приложения/весь экран
- ✅ Другой пользователь видит ваш экран

---

## 🐛 Отладка проблем

### Проблема: "0 участников"

**Проверка в консоли (F12):**

1. Ищите лог: `[useSocket] Room joined event received:`
   - ✅ Если есть → смотрите содержимое `participants`
   - ❌ Если нет → событие не пришло

2. Если событие пришло, но участников 0:
   - Проверьте: `[useSocket] Processing participants: X`
   - Проверьте: `[useSocket] Adding participant: ...`

3. Если участники добавляются, но не отображаются:
   - Проверьте: `[VideoGrid] Rendering: ...`
   - Проверьте значение `totalParticipants`

### Проблема: Видео не показывается

**Проверка:**

1. В лобби видео есть?
   - ✅ Да → перейдите к шагу 2
   - ❌ Нет → проверьте разрешения камеры

2. Проверьте логи при переходе в комнату:
   ```
   [LobbyPage] Joining room, current stream: {streamId}
   [RoomPage] Initializing local participant: hasStream: true
   ```

3. Если `hasStream: false`:
   - Возможно stream был очищен
   - Перезагрузите страницу

### Проблема: Чат не работает

**Проверка в консоли Network (F12 → Network):**

1. Отправьте сообщение
2. Ищите запрос: `chat:send`
3. Проверьте Response:
   - ✅ `success: true` → сообщение отправлено
   - ❌ `success: false` → смотрите `error`

### Проблема: Камера занята

**Симптом:** "Доступ к камере запрещен" при открытии второй вкладки

**Причина:** Браузер не дает двум вкладкам одновременно использовать камеру

**Решение:**
- Используйте РАЗНЫЕ браузеры (Chrome + Firefox)
- ИЛИ используйте инкогнито + обычный режим
- ИЛИ один пользователь отключает камеру перед запуском второго

---

## 📊 Ожидаемые логи в консоли

### При заходе в комнату:

```
[RoomPage] Loading room data for slug: a715-6716-4ba3
[RoomPage] Room data loaded: {id: "...", slug: "a715-6716-4ba3", ...}
[RoomPage] Joining room via socket...
[useSocket] joinRoom called for: a715-6716-4ba3
[useSocket] Room joined event received: {roomSlug: "a715-6716-4ba3", participants: Array(1)}
[useSocket] Current user ID: user-id-here
[useSocket] Processing participants: 1
[useSocket] Skipping self: user-id-here
[useSocket] Participants added. Total: 0
[useSocket] joinRoom callback response: {success: true}
[RoomPage] Successfully joined room
```

### Когда второй пользователь присоединяется:

**У первого пользователя:**
```
[useSocket] User joined: {userId: "user-2-id", displayName: "User Two", ...}
[useSocket] Adding participant: User Two user-2-id
🎉 User Two присоединился к комнате
```

**У второго пользователя:**
```
[useSocket] Processing participants: 1
[useSocket] Adding participant: User One user-1-id
[useSocket] Participants added. Total: 1
```

---

## ⚠️ Известные ограничения

1. **Один браузер** - только один пользователь может использовать камеру
2. **WebRTC** - работает только в локальной сети (localhost)
3. **STUN only** - нет TURN серверов, может не работать через NAT

---

## 🔧 Быстрые команды

### Остановить все:
```powershell
# Остановить фронтенд: Ctrl+C в терминале
# Остановить бэкенд: Ctrl+C в терминале
# Остановить базы данных:
cd server
docker-compose down
```

### Перезапустить базы данных:
```powershell
cd server
docker-compose restart
```

### Очистить данные:
```powershell
cd server
docker-compose down -v  # Удалит все данные!
docker-compose up -d
bun prisma migrate deploy
```

---

## 📝 Отчет о тестировании

После тестирования заполните:

- [ ] Регистрация работает для обоих пользователей
- [ ] Создание комнаты работает
- [ ] Второй пользователь может присоединиться по коду
- [ ] Оба видят друг друга в списке участников
- [ ] Видео отображается у обоих
- [ ] Чат работает в обе стороны
- [ ] Кнопки выкл. микрофона/камеры работают
- [ ] Расшаривание экрана работает
- [ ] При выходе пользователя другой видит уведомление

---

## 🆘 Если ничего не помогло

1. Очистите базы данных (команда выше)
2. Перезапустите все сервисы
3. Очистите кэш браузера (Ctrl+Shift+Delete)
4. Перезагрузите страницу (Ctrl+F5)
5. Проверьте что порты свободны:
   ```powershell
   netstat -ano | findstr :5000
   netstat -ano | findstr :5173
   netstat -ano | findstr :5433
   netstat -ano | findstr :6378
   ```
